{
  "name": "hf-vram-estimator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Zero-download LLM architecture and VRAM estimator for GitHub Pages and browser extensions",
  "scripts": {
    "dev": "BUILD_TARGET=web vite",
    "test": "node --no-warnings --test test/vram/*.test.js",
    "build:web": "BUILD_TARGET=web vite build",
    "build:ext": "BUILD_TARGET=ext vite build",
    "preview": "BUILD_TARGET=web vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "chart.js": "^4.4.6"
  }
}
