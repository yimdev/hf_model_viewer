{
  "name": "hf-vram-estimator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Zero-download LLM architecture and VRAM estimator",
  "scripts": {
    "dev": "vite",
    "test": "node --no-warnings --test test/app/*.test.js test/engine/*.test.js test/tree/*.test.js test/vram/*.test.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "chart.js": "^4.4.6"
  }
}
